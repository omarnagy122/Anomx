# Extra Python packages installed inside the Airflow image.
# Apache Airflow itself is provided by the base image in airflow/Dockerfile.
pandas==2.2.3
psycopg2-binary==2.9.10
kafka-python-ng==2.2.3
paho-mqtt==2.1.0
pyspark==4.1.0
